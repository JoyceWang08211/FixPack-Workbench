const $ = require('jquery'), React = require('react'), ReactDOM = require('react-dom');

let Comparator = React.createClass({
    render(){
        return (
            <div>
            </div>)
    }
});

module.exports = Comparator;